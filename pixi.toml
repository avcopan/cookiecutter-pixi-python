[workspace]
authors = ["avcopan <avcopan@gmail.com>"]
channels = ["conda-forge"]
name = "cookiecutter-pixi-python"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]
clean = "rm -rf ../project-name"
build = "cookiecutter . --no-input -o .."
setup = { cmd = "pixi run setup", cwd = "../project-name" }
pre-commit = { cmd = "pixi run pre-commit", cwd = "../project-name" }
docs-build = { cmd = "pixi run docs-build", cwd = "../project-name" }
readme = { cmd = "cat README.md", cwd = "../project-name" }
license = { cmd = "cat LICENSE", cwd = "../project-name" }
version = { cmd = "pixi run version", cwd = "../project-name" }

[tasks.all]
depends-on = [
    "clean",
    "build",
    "setup",
    "pre-commit",
    "docs-build",
    "readme",
    "license",
    "version",
]

[dependencies]
cookiecutter = ">=2.6.0,<3"
